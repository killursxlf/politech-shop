<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/png" sizes="32x32" href="/images/logo_polytechnic.png">
    <link rel="stylesheet" type="text/css" href="/fonts/barkentina/barkentina.css">
    <link rel="stylesheet" href="/styles/style.css">
    <script src="/JavaScript/product.js"></script>
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="main">
        <section class="hero">
            <div class="hero-text">
                <h1><%= title %></h1>
                <p>Стиль, комфорт, якість для кожного з Вас</p>
                <!-- Ссылка на страницу покупок -->
                <a href="/products/instock-page" class="hero-button">Перейти до покупок</a>
            </div>
        </section>
        <h1>Топ продажу</h1><br/>
        <section class="product-grid">
            <% products.forEach(product => { %>
                <div class="product">
                    <a href="/products/instock-page/<%= product.article %>">
                        <img src="<%= product.image %>" alt="Фото товара">
                        <h2><%= product.name %></h2>
                        <p><%= product.description %></p>
                        <p class="price"><%= product.price %> грн</p>
                    </a>
                </div>
            <% }) %>
        </section>
    </main>
</body>
</html>
