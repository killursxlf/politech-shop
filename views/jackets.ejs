<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/png" sizes="32x32" href="images/logo_polytechnic.png">
    <link rel="stylesheet" type="text/css" href="fonts/barkentina/barkentina.css">
    <link rel="stylesheet" href="styles/jackets_style.css">
    <script src="JavaScript/main.js"></script>
    <script src="JavaScript/product.js"></script>
</head>
<body>
    <%- include('partials/header') %>

    <div class="sort-container">
        <label for="sort">Сортування:</label>
        <select id="sort" onchange="sortProducts()">
            <option value="priceAsc">Ціна: за зростанням</option>
            <option value="priceDesc">Ціна: за спаданням</option>
        </select>
    </div>

    <div class="product-grid">
        <% jackets.forEach(function(product) { %>
        <div class="product">
            <a href="/jackets/<%= product.article %>">
                <img src="/images/jackets/<%= product.article %>.jpg" alt="<%= product.article %>">
                <h3><%= product.name %></h3>
                <p class="price" data-price="<%= product.price %>"><%= product.price %> грн</p>
            </a>
        </div>
        <% }); %>
    </div>
</body>
</html>
